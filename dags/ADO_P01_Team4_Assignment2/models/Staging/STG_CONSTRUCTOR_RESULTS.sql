SELECT
	CONSTRUCTORRESULTSID,
	RACEID,
	CONSTRUCTORID,
	POINTS,
    CASE WHEN STATUS = '\\N' THEN NULL ELSE STATUS END AS STATUS,  -- converting special characters to null
FROM {{ source('ASTON_MARTIN_DATA', 'CONSTRUCTOR_RESULTS') }}