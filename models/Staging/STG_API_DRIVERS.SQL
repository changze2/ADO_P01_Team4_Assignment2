SELECT *
FROM {{ source('ASTON_MARTIN_DATA', 'DRIVERS_API') }}
WHERE COALESCE(DRIVER_NUMBER, BROADCAST_NAME, FULL_NAME, NAME_ACRONYM, 
               TEAM_NAME, TEAM_COLOUR, FIRST_NAME, LAST_NAME, 
               HEADSHOT_URL, COUNTRY_CODE, SESSION_KEY, MEETING_KEY) IS NOT NULL
